package config

// Default shell configuration values.
// These are used when no user-defined value exists in ~/.dush/env.
const (
	DefaultPromptPrefix = "$"
	DefaultPromptSuffix = ">>"
	DefaultUserName     = "user"
)
